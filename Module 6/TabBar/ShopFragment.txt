import 'package:flutter/cupertino.dart';

class ShopFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
        child: Center(
          child: Text('Shop Fragment'),
        )
    );
  }
}