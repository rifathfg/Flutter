import 'package:flutter/cupertino.dart';

class BackpackFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
        child: Center(
          child: Text('BackpackFragment'),
        )
    );
  }
}