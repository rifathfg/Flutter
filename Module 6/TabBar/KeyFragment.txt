import 'package:flutter/cupertino.dart';

class KeyFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
        child: Center(
          child: Text('Key Fragment'),
        )
    );
  }
}